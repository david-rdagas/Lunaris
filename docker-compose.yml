services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lunaris
    restart: always
    stdin_open: true
    tty: true
    command: python sensors/barometer.py
    environment:
      - PYTHONPATH=/app